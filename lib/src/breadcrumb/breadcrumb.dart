import "package:flutter/material.dart";

class ZoBreadcrumb extends StatelessWidget {
  const ZoBreadcrumb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
